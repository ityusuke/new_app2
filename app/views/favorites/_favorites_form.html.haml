- if current_user.faved?(@tour)
  = link_to('お気に入り登録解除',tour_favorite_path(@tour.id),method: :delete,remote: :true)
- else
  = link_to('お気に入り登録する',tour_favorites_path(@tour.id),method: :post,remote: :true) 