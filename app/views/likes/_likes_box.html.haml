%p
  #{@likes.count}人のユーザーがいいねしています
-if liked?(@tour)
  = link_to('いいねを取り消す',tour_like_path(@tour.id),method: :delete,remote: :true)
-else
  = link_to('いいねする',tour_likes_path(@tour.id),method: :post,remote: :true)